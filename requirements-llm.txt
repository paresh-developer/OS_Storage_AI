# Optional: only needed for storage_ai/llm_config_extractor.py's local,
# CPU-only extractive-QA fallback (used only when an app's config file
# exists but doesn't parse as any structured format config_discovery.py
# understands). Nothing else in this app imports this file or requires
# it -- storage_ai.llm_config_extractor.is_available() returns False
# gracefully if it isn't installed, and app_discovery.py just skips that
# tier. ~1GB combined; install only if you want that fallback tier:
#
#   .venv/bin/pip install torch --index-url https://download.pytorch.org/whl/cpu
#   .venv/bin/pip install -r requirements-llm.txt
#
# The separate --index-url step keeps the download to the CPU-only torch
# build (~150-250MB) instead of the much larger CUDA-enabled default.
transformers==5.15.1
